; ArduinoMock – PlatformIO configuration
; Used for building and running native unit-tests.

[env:native]
platform = native
test_framework = unity
build_flags =
    -std=c++17
    -Wall
    -Wextra
    -I src
lib_deps =
    ${PROJECT_DIR}
